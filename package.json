{
  "name": "tokens-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Token Usage Leaderboard - The Kardashev Scale for AI Devs",
  "type": "module",
  "workspaces": [
    "packages/*",
    "web"
  ],
  "scripts": {
    "build": "cargo build --release --manifest-path cli/Cargo.toml -p tokens-cli && bun run build:cli",
    "build:cli": "bun run --cwd packages/cli build",
    "build:core": "cargo build --release --manifest-path cli/Cargo.toml -p tokens-cli",
    "cli": "./scripts/cli.sh",
    "dev:web": "bun run --cwd web dev"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
