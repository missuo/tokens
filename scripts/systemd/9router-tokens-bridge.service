[Unit]
Description=9Router → tokens gjc bridge (materialize today's usage)

[Service]
Type=oneshot
ExecStart=/usr/bin/python3 %h/.local/share/9router-tokens/9router_tokens_bridge_gjc.py
# The bridge script is read-only against the 9Router DB and only writes
# its own JSONL dir (~/.local/share/9router-tokens/sessions), so it is
# safe to run without elevated privileges or network access.
StandardOutput=journal
StandardError=journal
